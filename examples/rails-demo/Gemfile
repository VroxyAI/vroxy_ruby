# frozen_string_literal: true

source "https://rubygems.org"

gem "rails", "~> 8.0"
gem "puma"

# vroxy is not published to RubyGems (pre-launch, private).  Point
# at a checkout, or at the private repo over SSH.
gem "vroxy", path: "../.."
